[variables]
NIXPACKS_PYTHON_VERSION = "3.11"

[phases.install]
cmds = ["cd backend && pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "cd backend && gunicorn -k gevent -w 1 --bind 0.0.0.0:$PORT --timeout 120 --worker-connections 1000 app:app"
