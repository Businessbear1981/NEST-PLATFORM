[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn -k gevent -w 1 --bind 0.0.0.0:$PORT --timeout 120 --worker-connections 1000 app:app"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
